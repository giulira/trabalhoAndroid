<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_camera"
            android:icon="@drawable/ic_menu_camera"
            android:title="Import" />
        <item
            android:id="@+id/nav_gallery"
            android:icon="@drawable/ic_menu_gallery"
            android:title="Gallery" />
        <item
            android:id="@+id/nav_slideshow"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="Slideshow" />
        <item
            android:id="@+id/nav_manage"
            android:icon="@drawable/ic_menu_manage"
            android:title="Tools" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />

            <item
                android:id="@+id/nav_sobre"
                android:icon="@drawable/ic_menu_send"
                android:title="Sobre" />

            <item
                android:id="@+id/nav_novo_cadastro"
                android:icon="@drawable/ic_menu_send"
                android:title="Novo Cadastro" />

            <item
                android:id="@+id/nav_lista_cardapio"
                android:icon="@drawable/ic_menu_send"
                android:title="Lista de cardápio" />

            <item
                android:id="@+id/nav_mostrar_editar_deletar_cardapio"
                android:icon="@drawable/ic_menu_send"
                android:title="Editar / Excluir item do menu" />

            <item
                android:id="@+id/nav_sair"
                android:icon="@drawable/ic_menu_send"
                android:title="Encerrar" />
        </menu>
    </item>

</menu>
